services:
  node:
    image: "node:22"
    user: "node"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/home/node/app
    ports: # use if it is necessary to expose the container to the host machine
      - "8888:8888"
    command: ["npm", "start"] #start - siehe package.json JSON_Array_Converter.js wird per docker-compose up  ausgeführt ergebnis in console